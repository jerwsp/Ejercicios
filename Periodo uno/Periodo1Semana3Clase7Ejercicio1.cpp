# incluir  < iostream >
utilizando el espacio de  nombres  estándar ;

int  principal () {
	matriz < int , 5 > n;
	for ( tamaño_t i = 0 ; i < n. tamaño (); ++i )
	norte [ yo ] = 0 ;
	cout << " Elemento " << setw ( 13 ) << " Valor " << endl;
	for ( tamaño_t j = 0 ; j < n. tamaño (); ++j )
	cout << setw ( 7 ) << j << setw ( 13 ) << n [ j ] << endl;
	
}
